public class Old {

    /*for (int rowBlock1 = 15; rowBlock1 > 0; rowBlock1--) {

            // Moves the block downwards

            if (rowBlock1 == 1) {
                for (rowBlock1 = 1; rowBlock1 + 7 < 23; rowBlock1++) {
                    block1.drawRectangle(
                            new TerminalPosition(7, rowBlock1),
                            new TerminalSize(2, 7),
                            Symbols.BLOCK_MIDDLE);

                    screen.refresh();

                    // Erases when moving downwards
                    if (rowBlock1 != 1) {
                        block1.drawRectangle(
                                new TerminalPosition(7, rowBlock1 - 1),
                                new TerminalSize(2, 1),
                                ' ');

                        screen.refresh();
                    }

                    Thread.sleep(200);
                }
            }

            // Erases when moving upwards
            block1.drawRectangle(
                    new TerminalPosition(7, rowBlock1),
                    new TerminalSize(2, 7),
                    Symbols.BLOCK_MIDDLE);

            screen.refresh();


            // Moves the block upwards
            if (rowBlock1 < 16) {
                block1.drawRectangle(
                        new TerminalPosition(7, rowBlock1 + 7),
                        new TerminalSize(2, 1),
                        ' ');

                screen.refresh();
            }
            Thread.sleep(200);
        }*/


}
